#include <iostream>
using namespace std;

void signx(int x) {
	if (x > 0)
	{
		return 1;
	}
	else if (x==0)
	{
		return 0;
	}
	else {
		return -1;
	}
}

int main() {
	setlocale(LC_ALL, "rus");
	int a;
	cout << "Введите число: ";
	cin >> a;
	cout << endl;
	cout << signx(a);

}
